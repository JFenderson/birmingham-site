import { defineField, defineType } from "sanity";
import { createAccessibleImageField, createChapterSlugField, createPublishedAtField, createPublishedField, createSafeExternalUrlField } from "./shared.ts";
export const communityInitiative = defineType({ name: "communityInitiative", title: "Community Initiative", type: "document", fields: [defineField({ name: "title", type: "string", validation: (r) => r.required() }), defineField({ name: "slug", type: "slug", options: { source: "title" }, validation: (r) => r.required() }), defineField({ name: "subtitle", type: "string" }), defineField({ name: "body", type: "text", rows: 12, validation: (r) => r.required() }), createAccessibleImageField("heroImage", "Hero image", "Public initiative hero image."), defineField({ name: "gallery", type: "array", of: [{ type: "image", fields: [{ name: "alt", type: "string" }] }] }), createSafeExternalUrlField("actionUrl", "Action URL", "Optional HTTPS donation or participation link."), defineField({ name: "actionLabel", type: "string" }), createChapterSlugField("community initiative"), createPublishedField("community initiative"), createPublishedAtField("community initiative")] });
