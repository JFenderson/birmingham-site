import { defineField, defineType } from "sanity";
import { createAccessibleImageField, createChapterSlugField, createPublishedAtField, createPublishedField, createSafeExternalUrlField } from "./shared.ts";
export const signatureWeekend = defineType({ name: "signatureWeekend", title: "Signature Weekend", type: "document", fields: [defineField({ name: "title", type: "string", validation: (r) => r.required() }), defineField({ name: "slug", type: "slug", options: { source: "title" }, validation: (r) => r.required() }), defineField({ name: "overview", type: "text", rows: 6, validation: (r) => r.required() }), createAccessibleImageField("heroImage", "Hero image", "Public weekend hero image."), createSafeExternalUrlField("ticketUrl", "Ticket or registration URL", "HTTPS ticketing/payment link."), defineField({ name: "ticketLabel", type: "string", initialValue: "Buy tickets" }), defineField({ name: "schedule", type: "array", of: [{ type: "object", fields: [defineField({ name: "title", type: "string", validation: (r) => r.required() }), defineField({ name: "date", type: "datetime", validation: (r) => r.required() }), defineField({ name: "location", type: "string" }), defineField({ name: "description", type: "text", rows: 3 }), createAccessibleImageField("flyer", "Flyer", "Optional event flyer.")] }] }), createChapterSlugField("signature weekend"), createPublishedField("signature weekend"), createPublishedAtField("signature weekend")] });
